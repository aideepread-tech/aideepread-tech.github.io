### 🎙️ 第 07 頁：Contribution_1_Optimizers_as_Learning_Modules

#### 【本頁重點摘要】
*   Nested Learning (NL) 重新詮釋了優化器，視其為具備自身學習能力的「學習模組」，而非單純的訓練工具。
*   傳統優化器如「帶動量的隨機梯度下降法 (SGD with Momentum)」可被解構為一個雙層學習系統。
*   基於此洞見，我們可以通過升級優化器內部的「記憶模組」來設計更強大、更具表達力的優化器，例如 **Deep Momentum Gradient Descent (DMGD)**。

---

#### 【逐字講稿】

(開場白)
各位，我們通常認為優化器 (Optimizer) 就像是訓練模型的引擎，負責調整參數。但如果我告訴你，根據 Nested Learning 的觀點，這個引擎本身其實也在學習呢？這就是我們第一個驚人的貢獻：將優化器視為學習模組。

##### ① 解構傳統優化器

讓我們先從一個大家都很熟悉的例子開始：**帶動量的隨機梯度下降法 (SGD with Momentum)**。

從傳統視角看，它只是在梯度下降中加入了一個「動量」項，來加速收斂並越過局部最小值。但在 Nested Learning 的框架下，這其實是一個隱藏的**兩層優化問題 (2-level system)**。

*   **第一層 (外層)**，是我們熟知的模型權重 `W` 的更新，它的更新頻率相對較慢。
*   **第二層 (內層)**，則是那個動量項 `m`。論文揭示，這個動量項本身就是一個 **「無鍵值的聯想記憶體 (key-less associative memory)」**！它的唯一工作，就是學習如何將過去所有的「梯度歷史」壓縮並儲存在自己的參數中。它之所以是 "key-less"，是因為它對待所有梯度都一視同仁，把它們映射到一個單一的目標上。

> 這篇論文讓我們驚覺，我們習以為常的優化器，內部早已存在著一個記憶與學習的微系統。同樣地，論文也證明了另一個主流優化器 **Adam**，本質上也可以被看作是一個為模型梯度設計的「最佳聯想記憶體」。

##### ② 打造更強大的優化器

一旦我們接受了「優化器也是學習模組」這個觀點，一個全新的世界就此展開。我們不再需要盲目地發明新的優化器，而是可以像設計神經網路一樣，有系統地去「升級」它內部的記憶元件。

論文提出了三個具體的升級方向：

*   **第一，更具表達力的關聯 (More Expressive Association)**：既然動量是個記憶體，那我們可以給它更豐富的資訊。與其讓它是 "key-less" 的，我們可以引入「預處理 (preconditioning)」矩陣 `P_i`，讓記憶體學會將特定的「梯度 (key)」映射到有意義的「數值 (value)」。例如，這個 `P_i` 可以包含梯度的二階資訊，如 Hessian 矩陣的資訊，讓記憶更有方向性。

*   **第二，更具表達力的目標 (More Expressive Objectives)**：傳統動量的內部學習目標，類似於簡單的點積，這是一種較弱的學習規則。我們可以將它升級為更強大的 **L2 回歸損失**，這會產生一種基於 **「Delta Rule」** 的更新方式。這種規則能讓記憶體更有效地管理其有限的容量，從而更好地記住過去的梯度序列。

*   **第三，更具表達力的記憶體 (More Expressive Memory)**：這是最直接的想法。既然動量是一個線性的記憶體，那它的表達能力當然有限。我們何不直接用一個更強大的模組來取代它？例如，用一個 **MLP** 來取代線性的動量矩陣。這就誕生了論文中提到的 **「深度動量梯度下降法 (Deep Momentum Gradient Descent, DMGD)」**。

---

#### 【講者提示 & 轉場】
*   **節奏提醒**：講完「動量是無鍵值的聯想記憶體」後，可以稍作停頓。這是一個違反直覺但極具啟發性的觀點，給聽眾一點時間消化。
*   **補充案例**：如果時間允許，可以提到論文中另一個例子：將動量記憶體的輸出通過一個非線性函數（如 Newton-Schulz 迭代），就能推導出現代的 Muon 優化器。這再次證明了 NL 框架的解釋力。
*   **關鍵詞強調**：本頁的核心術語是 **`key-less associative memory`**、**`Delta Rule`** 和 **`DMGD`**。在講到這些詞時，請加重語氣，並確保聽眾理解它們分別對應了對記憶體「關聯」、「目標」和「架構」的升級。
*   **轉場橋樑 (Bridge)**：
    > 好的，我們剛剛看到 Nested Learning 如何讓我們重新解構並強化一個像「優化器」這樣的基礎元件。但 NL 的威力遠不止於此。如果連優化器本身都能學習，那模型架構是否也能學會「如何自我修改」呢？下一頁，我們就來探討這個更令人興奮的貢獻：自我修改的架構。